<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             mvvm:ViewModelLocator.AutowireViewModel="True"
             BackgroundColor="Teal"
             x:Class="Karty.Views.CreateKartPage">
    <StackLayout Margin="50">
        <Label Text="Add Kart"
               VerticalOptions="StartAndExpand"
               HorizontalOptions="Center"
               FontSize="Large"/>
        <Entry Placeholder="Kart name"
               Text="{Binding Kart.Name}"/>
        <StackLayout VerticalOptions="Center" Margin="20, 0, 0, 0">
            <Label Text="Members:"
               VerticalOptions="StartAndExpand"
               HorizontalOptions="Center"
               FontSize="Large"/>
            <Entry Placeholder="User email to invite"
               Text="{Binding UserEmail}"/>
            <Button Text="Add Member"
                Command="{Binding AddMemberCommand}"
                VerticalOptions="End"/>
        </StackLayout>
        <Button Text="Create"
                WidthRequest="200"
                HeightRequest="50"
                Command="{Binding AddKartCommand}"
                VerticalOptions="EndAndExpand"/>
    </StackLayout>
</ContentPage>